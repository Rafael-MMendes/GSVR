Docker-compose up -d
Docker-composse up -d --build
docker-compose up -d --build backend
docker-compose up -d --build frontend
docker exec -it ft-backend npm run import
docker exec -it ft-backend npm run users